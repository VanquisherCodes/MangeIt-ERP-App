spring.application.name=manageit-backend
server.port=${port.http:${SERVER_PORT:8080}}

# Fill these with your Azure ML endpoint values for local testing.
azure.ml.scoring-url=${AZURE_ML_SCORING_URL}
azure.ml.api-key=${AZURE_ML_API_KEY}
