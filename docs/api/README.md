# API Contracts

Use this folder for backend integration references:
- endpoint list and methods
- request/response JSON samples
- auth token and session policy
- error code standards
- versioning notes (`/v1`, `/v2`, etc.)
- ML prediction backend bridge contract
